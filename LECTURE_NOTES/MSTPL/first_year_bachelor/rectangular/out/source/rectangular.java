/* autogenerated by Processing revision 1293 on 2024-03-19 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class rectangular extends PApplet {

float w = 800;
float h = 800;
int row = 50;
int col = 50;
Block[][] blocks = new Block[row][col];
float dim = w / row;

public void setup() {
    /* size commented out by preprocessor */;
    rectMode(CENTER);

    float offset = (dim / 2);

    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            blocks[i][j] = new Block(i * dim + offset, j * dim + offset, dim - 3);
        } 
    }

}

public void draw() {
    background(0);

    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            blocks[i][j].display();
        } 
    }

    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            blocks[i][j].update(mouseX, mouseY);
        } 
    }
    
    
}
class Block {

    float x;
    float y;
    float dim;
    float angle;

    Block(float posx, float posy, float dimension) {
        this.x = posx;
        this.y = posy;
        this.dim = dimension;
        this.angle = 0.0f;
    }

    public void display() {
        pushMatrix();
        translate(this.x, this.y);
        rotate(this.angle);
        rect(0, 0, this.dim, this.dim);
        popMatrix();
    }

    public void update(float x, float y) {

        float d = dist(x, y, this.x, this.y);
        if (d <= (this.dim / 2) + 30) {
            this.angle += 0.05f;
        }

        if (this.angle > 0.0f && this.angle < HALF_PI) {
                this.angle += 0.05f;
            } else {
                this.angle = 0.0f;
            }

        this.angle %= TWO_PI;

    }
                        
}


  public void settings() { size(800, 800); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "rectangular" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
